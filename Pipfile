[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
pytest = "*"
cookiecutter = "*"
pytest-cookies = "*"
pyyaml = "*"
safety = "*"

[requires]
python_version = "3.13"

[scripts]
test = "pytest -s -v --ff --doctest-modules tests"
